# Component images

Vendored here so the tool has no external dependency at runtime.

Populate them once with:

```
npm run fetch-assets
```

then commit the PNGs. Any image that is missing renders as a red placeholder
naming the part, so a gap is visible rather than silent.
